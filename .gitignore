# OS / editor cruft
Thumbs.db
.DS_Store
*.swp

# Node (only if serve/tooling is ever added)
node_modules/

# Copyrighted source material — NEVER commit. Used locally only for
# verifying/authoring trivia. The PDF and any extracted text stay private.
_sources/
